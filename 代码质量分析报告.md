# 📊 Prompt Forge 项目代码质量分析报告

### 一、项目概览

| 项目 | 详情 |
|------|------|
| 项目名称 | Prompt Forge |
| 定位 | 浏览器端 AI 提示词管理工具 |
| 文件数 | 7 个核心文件 |
| 总代码量 | ~5000 行 (JS + CSS + HTML + Shell) |
| 运行方式 | Node.js 本地服务器 + 浏览器前端 |

**核心模块：**

| 文件 | 大小 | 职责 |
|------|------|------|
| `app.js` | ~2900 行 | 原子提示词管理（分类/标签/画布/解析/API集成） |
| `inspiration.js` | ~750 行 | 灵感图片收藏（瀑布流/标签联想/数据同步） |
| `start.command` | ~227 行 | Node.js 本地服务器（数据持久化/API路由/图片服务） |
| `style.css` | ~500 行 | 主界面样式 |
| `inspiration.css` | ~350 行 | 灵感页面样式 |
| `index.html` | ~200 行 | HTML 结构 |

---

### 二、架构设计分析

#### 2.1 整体架构（评分：⭐⭐⭐⭐ 4/5）

```
┌─────────────────────────────────────────────────────┐
│                   index.html                        │
├──────────────────────┬──────────────────────────────┤
│   app.js (2900行)     │  inspiration.js (750行)       │
│   - 分类/标签管理       │  - 瀑布流布局                 │
│   - 画布组装           │  - 图片收藏                   │
│   - 提示词解析炸开     │  - 标签联想                   │
│   - 拖拽排序           │  - 数据同步                   │
│   - AI API 集成       │                              │
├──────────────────────┴──────────────────────────────┤
│  localStorage (前端缓存) ←→ start.command (Node.js) │
│                            ↓                        │
│                      JSON 文件 + 图片 (本地磁盘)      │
└─────────────────────────────────────────────────────┘
```

**优点：**
- inspiration.js 采用 IIFE 自包含模块，不与 app.js 的内部变量耦合，设计合理
- 数据持久化采用「本地缓存 + 服务端 JSON」双写策略，兼顾速度和可靠性

**问题：**
- app.js 严重膨胀（~2900行），缺乏模块拆分
- 与 inspiration.js 之间通过 `window.switchMode` 等全局变量通信，耦合性较高
- 没有使用任何构建工具或模块系统（ES Modules / bundler）

#### 2.2 数据流设计（评分：⭐⭐⭐ 3.5/5）

```
用户操作 → 内存(Array/Object) → localStorage.setItem → fetch POST → JSON文件
初始化   → fetch GET → JSON文件 → localStorage.setItem → 内存(Array/Object)
```

**优点：**
- 实时同步：数据变更立即写 localStorage，并 debounce 50ms 后 POST 到服务端
- 重置后通过 sessionStorage 标记防止旧数据回流，逻辑周全
- 数据目录可动态配置

**问题：**
- 双写策略存在不一致风险：如果 POST 失败，localStorage 和 JSON 文件会不同步，但代码只是 `.catch(() => {})` 静默忽略
- `Storage.set` 的 debounce 逻辑依赖全局 `_flushTimer`，如果用户快速操作后关闭页面，`beforeunload` 中的 `flush()` 无法保证在 mac Safari 中一定执行
- 缺乏数据版本号或迁移路径，如果 JSON 结构未来变更，没有 schema migration 机制

---

### 三、代码质量分模块评估

#### 3.1 `app.js`（评分：⭐⭐⭐ 3/5）

**主要问题：**

##### 3.1.1 文件过于庞大（~2900行）
`app.js` 承担了分类管理、标签管理、画布组装、拖拽排序、AI 解析、图片反推、API 配置管理等职责，严重违反单一职责原则。

**建议拆分：**
```
app.js →
  └── modules/
      ├── storage.js      # 数据持久化 (Storage object)
      ├── categories.js   # 分类管理 (tabs/编辑/删除)
      ├── tags.js         # 标签管理 (library/cards)
      ├── canvas.js       # 画布操作 (chip/render/drag)
      ├── drag-drop.js    # 通用拖拽系统 (ghost/插入条)
      ├── parser.js       # 提示词解析 (split/AI/match)
      ├── api.js          # AI API 调用 (callOpenAI/embedding)
      └── ui.js           # UI 辅助 (toast/confirm/modal)
```

##### 3.1.2 全局变量过多
```javascript
// app.js 中有大量全局变量：
let tags = [], categories = [], canvasTags = [];
let currentCatId, catEditMode, libEditMode, showEn = false;
let _catDrag = {}, _libDrag = {}, _canvasDrag = {}, _emptyDrag = {};
let dragFromLibrary = null;
let _parseAtoms = [];
let _reverseImageData = null;
let _undoStack = [], _undoTimer;
let apiConfigs = [], activeApiId = null;
// ... 还有更多
```

**建议：** 至少封装到一个 `App` 命名空间或类中。

##### 3.1.3 重复代码较多

**拖拽系统重复：** `_catDrag`、`_libDrag`、`_canvasDrag` 三个拖拽系统的逻辑高度相似（mousemove/mouseup 处理、ghost 创建、插入条更新），可以抽象为一个通用拖拽管理器。

**读取-检查-写入模式重复：**
```javascript
// 这种模式出现了数十次：
const arr = Storage.get("promptForge.collapsedSubs", []);
const i = arr.indexOf(groupId);
if (i >= 0) arr.splice(i, 1); else arr.push(groupId);
Storage.set("promptForge.collapsedSubs", arr);
```

可以封装为：
```javascript
function toggleSetItem(key, value) {
  const arr = Storage.get(key, []);
  const idx = arr.indexOf(value);
  idx >= 0 ? arr.splice(idx, 1) : arr.push(value);
  Storage.set(key, arr);
}
```

##### 3.1.4 内联 HTML + onclick 污染
```javascript
// 行 2466-2482：内联 onclick 绑定
el.innerHTML = apiConfigs.map(cfg => {
  return `<div ...>
    <button onclick="__apiToggle(${safeId})">...</button>
    <button onclick="__apiEdit(${safeId})">...</button>
  </div>`;
}).join("");
```
这导致需要在 `window` 上挂载 `__apiToggle`、`__apiEdit`、`__apiDel` 函数，且引号转义容易出错。应使用事件委托。

##### 3.1.5 自带 MD5 实现
行 1521-1578 实现了完整的 MD5 算法，用于百度翻译签名。虽然代码正确，但在现代环境中，`crypto.subtle.digest('SHA-256')` 是更好的选择。不过考虑到百度翻译 API 要求 MD5，可以理解。

##### 3.1.6 JSON 解析鲁棒性不足
行 1886-1899 的 JSON 解析尝试了几层降级，但没有处理 `content` 为 `undefined` 或 `null` 的情况，直接调 `content.trim()`。虽然 AI 响应的 content 通常非空，但缺乏防御。

---

#### 3.2 `inspiration.js`（评分：⭐⭐⭐⭐ 4/5）

**优点：**
- IIFE 封装，变量不污染全局，独立性好
- 瀑布流用 JS flex columns 实现，避免了 CSS column-count 的水平溢出问题
- 标签联想功能设计合理：始终可见的 chip 列表，优于下拉菜单
- `resolveImageSrc` 有完善的降级策略：File System Access API → `/img/` HTTP → 空

**问题：**

##### 3.2.1 数据同步策略有缺陷
```javascript
// syncFromAPI() 中有比较奇怪的空数据判断逻辑：
if (items.length > 0) {
  // 服务器有数据 → 直接用
  localStorage.setItem(LS_ITEMS, JSON.stringify(items));
} else if (localItems.length > 0 && !sessionStorage.getItem('_migrated_insp')) {
  // 服务器空 + 本地有 + 未迁移 → 迁移
  saveItems(localItems);
} else {
  // 服务器空 + 本地空 或 已迁移 → 清空本地
  localStorage.removeItem(LS_ITEMS);
}
```
这个逻辑的问题是：如果服务器 JSON 文件被手动清空（非重置），且本地有数据且 sessionStorage 有迁移标记，本地的数据会被清空。更好的做法是：服务器数据优先，但服务器为空时不主动清空本地，除非是重置操作（可以通过一个特殊的 reset 标记来区分）。

##### 3.2.2 `getAllTags()` 效率问题
```javascript
function getAllTags() {
  const set = new Set();
  getItems().forEach(it => (it.tags || []).forEach(t => set.add(t)));
  return Array.from(set).sort((a, b) => a.localeCompare(b, 'zh'));
}
```
每次调用都会遍历所有 items 并重新排序。在 tag input 的每次 `input` 事件中都会调用。建议缓存结果。

##### 3.2.3 File System Access API 降级设计
使用了 IndexedDB 持久化目录句柄 + File System Access API，这个设计对 Chromium 浏览器有效，但在 Safari 中不可用。代码做了良好的降级处理（回退到 `/img/` HTTP 路由），这点很好。

---

#### 3.3 `start.command`（评分：⭐⭐⭐ 3/5）

**优点：**
- 单文件 Node.js 服务器，零依赖，启动简单
- 动态文件路径函数，支持运行时切换数据目录
- 路径遍历防护：`/img/` 路由检查了 `fp.startsWith(dataDir)` 防止目录穿越

**问题：**

##### 3.3.1 错误处理不完善
```javascript
// parse JSON 没有 try-catch
const items = JSON.parse(body);  // 行 94，如果 body 不是合法 JSON 会崩溃
```

##### 3.3.2 重置 API 只删除文件，不处理子目录
```javascript
fs.readdirSync(dataDir).forEach(f => {
  try { fs.unlinkSync(path.join(dataDir, f)); } catch (_) {}
});
```
如果数据目录中有子目录（虽然目前没有），会尝试用 `unlinkSync` 删除目录导致报错。应检查 `f` 是否为文件再删除。

##### 3.3.3 旧文件迁移逻辑只在初始化时运行
```javascript
// 行 44-51：只检查一次
['insp-items.json', 'insp-config.json', 'app-data.json'].forEach(f => {
  const oldFile = path.join(ROOT, f);
  const newFile = path.join(dataDir, f);
  if (fs.existsSync(oldFile) && !fs.existsSync(newFile)) {
    fs.copyFileSync(oldFile, newFile);
  }
});
```
如果用户在数据目录切换后重新启动，旧的 JSON 文件可能不会被迁移到新目录。

##### 3.3.4 端口冲突处理简单粗暴
```bash
lsof -ti:3000 | xargs kill -9 2>/dev/null
```
直接 kill 端口上的所有进程，这可能会误杀其他使用 3000 端口的应用。

---

#### 3.4 CSS 质量（评分：⭐⭐⭐⭐ 4/5）

**优点：**
- 使用 CSS 自定义属性（`--bg`, `--text`, `--accent` 等）实现主题变量
- 布局使用 flexbox，兼容性好
- 响应式设计，支持面板拖拽调整宽度
- 隐藏滚动条等细节处理得当

**问题：**
- `style.css` 和 `inspiration.css` 中有少量重复样式定义
- 部分 CSS 选择器嵌套过深，如 `.insp-detail .insp-detail-body .insp-detail-right .insp-detail-actions` 这种长选择器

---

### 四、功能实现深度分析

#### 4.1 拖拽排序（评分：⭐⭐⭐⭐ 4/5）

实现了完整的 ghost 拖拽 + 插入条定位系统，涵盖三个区域（分类/词库/画布）+ 空标签拖拽。

**精妙之处：**
- `updateInsertBar()` 使用缓存的 `getBoundingClientRect()` 避免重复 reflow
- rAF 节流全局 mousemove，避免高频重绘
- `is-dragging` 延迟添加，避免纯点击时闪烁
- 支持同组重排和跨组移动（词库卡片拖拽）

**可改进：**
- 三个拖拽系统大量重复代码，可抽象为通用拖拽类
- ghost 创建使用了 `cloneNode(true)` 和内联样式重置，在复杂 DOM 下可能有性能问题

#### 4.2 AI 提示词解析（评分：⭐⭐⭐ 3.5/5）

支持：
- 规则拆分（自定义分隔符，如逗号/空格等）
- AI 语义拆分（调用 LLM）
- 向量匹配（embedding 余弦相似度）
- 候选标签推荐 + 一键入库

**精妙之处：**
- `callOpenAI` 同时支持普通调用和流式输出（`onStream` 回调），复用度高
- 自定义分隔符的正则转义处理恰当
- embedding 缓存（`embCache`），避免重复计算

**可改进：**
- AI 解析的 prompt 是硬编码的，虽然有设置面板，但 prompt 模板可以更灵活
- Stream 处理中 `line.slice(6)` 直接截取，如果 SSE 行不以 `data: ` 开头会出错（虽已过滤）
- 向量匹配的结果缓存（`embCache`）使用 `localStorage`，大型词库可能导致存储空间不足（localStorage 限制 5MB）

#### 4.3 数据持久化（评分：⭐⭐⭐ 3.5/5）

**优点：**
- localStorage 实时写入 + debounced POST 到服务端
- sessionStorage 迁移标记防止重置后回流
- 数据目录动态可配
- `/api/reset` 完整清除所有数据

**问题：**
- 并发写入风险：如果用户同时操作两个标签页，JSON 文件可能被覆盖
- 没有实现乐观锁或版本号检测
- debounce 50ms 的 POST 可能丢失最后的写入（如果页面快速关闭）

---

### 五、安全性分析

| 项目 | 状态 | 说明 |
|------|------|------|
| XSS (存储型) | ⚠️ 部分防御 | `escapeHtml()` 用于标签名和提示词，但部分地方（如 `canvasTags` 的 `cn`）渲染时未转义 |
| XSS (innerHTML 注入) | ⚠️ 中等风险 | 多处使用 `el.innerHTML = ...` 拼接用户数据，虽然有 `escapeHtml` 但不统一 |
| 服务器路径遍历 | ✅ 良好 | `/img/` 路由有 `fp.startsWith(dataDir)` 检查 |
| API 密钥泄露 | ⚠️ 中等风险 | 密钥存储在 localStorage 中，以明文传输 |
| CORS | ✅ 已配置 | `Access-Control-Allow-Origin: *` |

**具体 XSS 风险点：**
```javascript
// app.js 行 898-907：renderCanvas 中
return `<div class="tag-chip..." data-idx="${i}">
  ${showEn && t.en ? t.en : t.cn}<span class="remove" ...>×</span>
</div>`;
```
此处的 `t.cn` 和 `t.en` 没有经过 `escapeHtml()` 转义，如果用户输入包含 `<script>` 等标签，可能导致 XSS。

---

### 六、性能分析

| 项目 | 评分 | 说明 |
|------|------|------|
| DOM 操作 | ⭐⭐⭐ | `renderLibrary()` 经常全量重渲染，大数据量时性能堪忧 |
| 事件处理 | ⭐⭐⭐⭐ | 事件委托合理，rAF 节流拖拽 |
| 数据读写 | ⭐⭐⭐ | localStorage 同步操作频繁，可能阻塞主线程 |
| 图片加载 | ⭐⭐⭐⭐ | lazy loading，blob URL 缓存 |
| 网络请求 | ⭐⭐⭐⭐ | debounce 合并写入请求 |

**关键性能瓶颈：**
1. `renderLibrary()` 每次搜索输入都完整重建 DOM，建议使用虚拟滚动或 requestAnimationFrame 分批渲染
2. `embCache` 存储在 localStorage 中，大词库时获取/设置可能阻挡渲染

---

### 七、改进建议优先级

#### 🔴 高优先级（建议立即修复）

1. **XSS 防护不统一** — 所有渲染用户数据的地方统一使用 `escapeHtml()`
2. **app.js 代码拆分** — 将 2900 行文件拆分为多个模块
3. **服务器 JSON parse 异常处理** — `JSON.parse(body)` 应包裹 try-catch

#### 🟡 中优先级（建议近期改进）

4. **抽象拖拽系统** — 将三个拖拽实现合并为一个通用类
5. **数据同步容错** — POST 失败时加入重试机制和错误提示
6. **inspiration.js 的标签缓存** — `getAllTags()` 结果缓存，避免重复计算
7. **全局变量封装** — 将 app.js 的全局状态封装到 App 命名空间或类中

#### 🟢 低优先级（可选优化）

8. **引入 ES Modules** — 使用 `import/export` 替代全局变量
9. **虚拟滚动** — 大词库情况下的 renderLibrary 性能优化
10. **加入 linter/formatter** — ESLint + Prettier 保证代码风格一致
11. **单元测试** — 核心逻辑（fuzzyMatch、splitPrompt、matchAtom）添加测试
12. **MD5 替换为 Web Crypto API** — 使用 `crypto.subtle.digest`

---

### 八、总结

| 维度 | 评分 | 评语 |
|------|------|------|
| 架构设计 | ⭐⭐⭐⭐ | 整体清晰，数据流合理，局部模块化不足 |
| 功能完整性 | ⭐⭐⭐⭐⭐ | 分类/标签/画布/解析/AI/收藏/导出 功能齐全 |
| 代码质量 | ⭐⭐⭐ | 单文件过大，重复代码多，部分安全性不足 |
| 用户体验 | ⭐⭐⭐⭐ | 交互细节到位（拖拽反馈、toast、撤销） |
| 数据安全 | ⭐⭐⭐ | 多节点同步可能丢失数据，缺少冲突检测 |
| 可维护性 | ⭐⭐⭐ | 模块拆分后可大幅提升 |
| **综合评分** | **⭐⭐⭐½** | 功能强大但需工程化重构 |

**一句话总结：** 这是一个功能强大、设计用心的个人工具项目，拥有出色的 UI 交互细节和完整的 AI 工作流集成。最大的短板在于 app.js 的单体文件结构和缺乏模块化，建议优先进行代码拆分和安全加固。
